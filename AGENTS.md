# Agent Instructions

Read `/home/jake/Projects/agent-scripts/AGENTS.MD` before working here.

## Workspace Rules

- Keep this repository as the suite shell and shared-contract layer.
- Do not import or merge standalone dinosaur tools unless explicitly asked.
- Prefer small shared crates over broad framework code.
- Keep user-visible behavior documented in `README.md` or `docs/`.
- No `unwrap()` or `expect()` in library code.


# Changelog

## Unreleased

- Created the initial Dino Tools suite shell with `dino-cli`, `dino-core`, and
  `dino-io`.
- Registered `trex` and `microraptor` as external tools instead of importing
  their standalone repositories.
- Added the first external tool slot documentation for Microraptor.
- Added workspace readiness gates and unpublished crate metadata.

# Changelog

## Unreleased

- Created the initial Dino Tools suite shell with `dino-cli`, `dino-core`, and
  `dino-io`.
- Registered `trex` as an external tool instead of importing its standalone
  repository.
- Promoted microraptor into the workspace as `dino-seq` (`crates/dino-seq`).
- Added workspace readiness gates and unpublished crate metadata.

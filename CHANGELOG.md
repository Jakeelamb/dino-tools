# Changelog

## Unreleased

- Pivoted Dino Tools from a suite wrapper to a standalone-tool workspace.
- Removed the `dino` umbrella CLI, registry metadata crate, shared IO helper
  crate, and wrapper-oriented docs.
- Kept `dino-seq` and `dna` as independent packages with their own public
  surfaces.

# Tool Slots

This directory records tools that belong under the Dino Tools suite identity.

An external tool slot is documentation and registry metadata only. It should
name the standalone source repository, command, and suite-facing contracts
without importing implementation code.

Tool code should move into Dino Tools only when the shared contract is stable
and at least one other suite component needs it.
